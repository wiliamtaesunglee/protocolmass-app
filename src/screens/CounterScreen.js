import React from 'react'
import {View, Text, StyleSheet, Button} from 'react-native'

const CounterScreen = () => {
    retrurn(
        <View>
            <Text>
                counter screen:
            </Text>

            <Button 
            title='press to incress + 1'
            />
        </View>
    )
}

const styles = StyleSheet.create({

}) 

export default CounterScreen;